{{--
    TASK 4 of "close the delivery loop" — the question a thesis panel
    (or anyone new to this dashboard) asks first: what's the difference
    between these two numbers? Shared by both dashboards so the wording
    can never drift between them. Two sentences each, on purpose — this
    is an orientation, not documentation.
--}}
<div class="bg-white rounded-lg shadow-sm p-4 mb-4 grid grid-cols-1 md:grid-cols-2 gap-4 text-xs text-gray-600">
    <div>
        <p class="font-semibold text-gray-700 mb-1"><i class="bi bi-clipboard-data text-brand-600"></i> In-Term Status</p>
        <p>One subject, one term, from the assessment evidence entered so far. Available immediately, and it changes as more evidence is added.</p>
    </div>
    <div>
        <p class="font-semibold text-gray-700 mb-1"><i class="bi bi-graph-up-arrow text-purple-600"></i> Risk Level</p>
        <p>Every subject, using the trend across terms, produced by the classifier from the submitted term report. Available once that report is submitted.</p>
    </div>
</div>
